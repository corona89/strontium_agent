# 개발 환경 (SQLite)
DATABASE_URL=sqlite+aiosqlite:///../db/app.db

# 운영 환경 (MySQL / MariaDB)
# DATABASE_URL=mysql+aiomysql://user:password@host:3306/dbname

JWT_SECRET=your-secret-key-here  # secrets.token_hex(32) 로 생성
COOKIE_SECURE=False  # 운영 환경에서는 True
